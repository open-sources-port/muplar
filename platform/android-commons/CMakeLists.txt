# Header-only Android support lives here:
# - root: small host-side Android/ELF compatibility headers.
# - android/: NDK C++ binder wrapper headers used by generated AIDL fixtures.
# - libcpp-config/: fixture-only libc++ config override; do not expose to host builds.
